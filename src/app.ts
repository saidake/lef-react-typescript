import React from 'react';
import ReactDOM from 'react-dom';
import './app.scss';


const template =React.createElement('div',null,'This is a lef typescript project');

ReactDOM.render(template, document.getElementById('root'));